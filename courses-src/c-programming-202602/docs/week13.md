# 13주차 · 포인터 활용
> C언어 · 미래모빌리티학과 | CLO2 | 교재 Ch13 · 자료 `lectures/14`

## 학습 목표
- call-by-reference로 함수가 바깥 변수를 직접 바꾼다.
- 포인터와 배열의 관계, 포인터 배열, 함수 포인터를 사용한다.
- 여러 결과를 포인터 매개변수로 동시에 반환한다.

## 강의 흐름 (150분)
| 시간 | 활동 | 내용·자료 |
|------|------|-----------|
| 10 | 복습/도입 | 값 전달 vs 주소 전달의 차이 |
| 45 | 개념 | call-by-reference, 포인터·배열 관계, 함수 포인터 (Ch13) `lectures/14` |
| 25 | 시연 | swap·합/평균 동시 반환 `ex05_pointer.c` |
| 45 | 실습 | 센서 보정 함수(call-by-ref), 최댓값+인덱스 반환 |
| 25 | 정리 | 함수 포인터 디스패치 테이블 소개, 과제 공지 |

## 모빌리티 연계
- 센서값 보정/필터를 "값을 고쳐 돌려주는" 함수로 — 제어 파이프라인의 기본.

## 과제 (13주차)
- 합·평균 동시 반환, 최댓값+인덱스 반환(연습 5-2~5-3), (도전) 함수 포인터 테이블.

## 자료
- `lectures/14/` (포인터 활용, 실습, 도전문제, Understanding Pointer Arrays PDF/PPTX)

## 형성평가
- ☐ call-by-ref 구현 ☐ 다중 반환(포인터) ☐ 포인터=배열 관계 설명
