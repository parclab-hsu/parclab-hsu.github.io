# 7주차 · 함수 + 시리얼 명령 제어 (중간 정리)
> C언어 · 미래모빌리티학과 | CLO1·CLO3 | 교재 Ch08 · 자료 `lectures/09`

## 학습 목표
- 함수 정의/선언/호출, 매개변수·반환값, 함수 분할의 이점을 설명한다.
- 제어 로직을 함수로 모듈화한다.
- **시리얼 명령(문자)→동작** 디스패치를 구현한다(함수 포인터 맛보기).

## 강의 흐름 (150분)
| 시간 | 활동 | 내용·자료 |
|------|------|-----------|
| 10 | 도입 | 중복 코드의 문제 → 함수의 필요 |
| 40 | 개념 | 함수 정의/호출, 매개변수·반환, 프로토타입 (Ch08) `lectures/09` |
| 25 | 시연 | 명령 문자→함수 호출 구조 `code/arduino/09_face_main/` |
| 40 | **아두이노 실습** | 시리얼로 `h/w/a/o/n` 입력→표정 전환 |
| 35 | 중간정리 | 1~7주 핵심 요약, 중간고사 범위 안내, Q&A |

## 모빌리티·SW공학 연계
- 함수 모듈화 = SW공학의 출발점(유지보수·재사용). 명령 디스패치는 ROS 콜백 구조의 전조.

## 과제 (7주차)
- 제어 로직 3개 이상 함수 분리, 시리얼 명령 제어. (도전) 함수 포인터 테이블.

## 자료
- `lectures/09/` (함수, 함수 실습, 함수 실습 과제 PDF/PPTX)
- 코드 `code/arduino/09_face_main/{face_main.ino,config.h}`

## 형성평가
- ☐ 함수 분할 기준 설명 ☐ 매개변수/반환 정확 ☐ 명령→동작 전환 동작
